# Minecraft Server Configuration
# Copy this file to .env and adjust the values before starting the server.
# cp .env.template .env

# Minecraft Server Properties
# All variables prefixed with MC_ are automatically written to server.properties
# Add new settings with MC_<SETTING>=<VALUE> (e.g. MC_MAX_PLAYERS=20)
MC_DIFFICULTY=normal